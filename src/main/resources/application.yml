spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3308/boarddb?serverTimezone=Asia/Seoul
    username: root
    password: root

  thymeleaf:
    prefix: classpath:/static/
    suffix: .html

  jackson:
    property-naming-strategy: SNAKE_CASE

  jpa:
    show-sql: true